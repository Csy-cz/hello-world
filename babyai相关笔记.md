![image-20260812162402387](C:\Users\HUAWEI\AppData\Roaming\Typora\typora-user-images\image-20260812162402387.png)

我用最直白的方式给你讲，不讲术语，就类比成「你玩游戏，大模型帮你按键盘」。

## 一、整件事到底在干嘛

你可以把 BabyAI 理解成一个**格子走迷宫小游戏**：

- 有一个小人在房间里
- 房间里有墙、球、箱子这些东西
- 任务是「走到红色的球旁边」
- 你只能做 7 个操作：左转、右转、往前走、捡东西、放东西、开门、说我完成了

我们写这个程序，目的就是：

**不让人玩，让大模型来玩。程序负责把游戏画面翻译成文字给大模型看，大模型说按哪个键，程序就帮它按下去。**

------

## 二、四段代码，分别干四件事

### 1. 第一段：按键对照表

python

```
ACTION_MAP = {
    0: "左转", 1: "右转", 2: "前进",
    3: "拾取物体", 4: "放下物体", 5: "开关/交互", 6: "完成任务"
}
```

**作用**：游戏只认数字（按 0 就是左转，按 2 就是前进），大模型只认识中文。这张表就是「数字 ↔ 中文」的翻译字典。

- 游戏给程序数字 → 程序查表，告诉你 “刚才执行了前进”
- 大模型说 “前进” → 程序查表，换成数字 2 发给游戏

### 2. 第二段：把游戏画面 “念” 给大模型听

函数名叫 `state_to_text`，你可以理解成**解说员**。

游戏画面是程序内部的数据，大模型看不懂。这个函数就干一件事：

把当前游戏里所有信息，读出来拼成一句句中文，比如：

- 现在任务是什么
- 小人站在第几格、面朝哪边
- 小人正前方是墙还是空地（能不能往前走）
- 房间里哪个位置有什么颜色的球、箱子

然后把这段中文发给大模型。

> 你之前遇到 “一直原地按前进”，就是因为最早的版本没告诉大模型 “前面有箱子，走不动”。大模型以为前面是空的，就一直按前进，结果小人动不了。

### 3. 第三段：问大模型该按哪个键

函数名叫 `llm_decide`，你可以理解成**打电话问高手**。

它做三件事：

1. 把刚才拼好的中文画面，加上一句规则（“只能选这 7 个动作，输出要按格式来”），一起发给大模型
2. 等大模型回复，比如大模型说 “我觉得应该右转”
3. 从回复里把动作名抠出来，再通过对照表换成数字，准备发给游戏

加了很多容错：

- 大模型说错话、格式不对 → 程序自己猜一个动作，不让程序崩掉
- 网络卡了、API 报错 → 也默认给一个动作，继续跑

### 4. 第四段：主循环，一遍一遍重复玩

就是最下面的 `while` 循环，相当于**一局游戏的流程**：

1. 开一局新游戏，生成随机房间
2. 走第一步：
   - 解说员念画面
   - 问大模型按什么
   - 帮它按下去
   - 小人动一下，游戏画面更新
3. 走第二步、第三步…… 一直重复
4. 什么时候停：
   - 走到红球旁边，任务成功 → 停
   - 走满 100 步还没成功 → 停
   - 游戏判定结束了 → 停

------

## 三、拿一步完整举例，你对照着日志就能看懂

比如你日志里某一步：

1. **游戏当前状态**：小人在 (4,5)，脸朝上，正前方 (4,4) 有个红箱子，走不动
2. **解说员拼文字**：“你在 (4,5)，朝上，正前方有箱子，不能前进，红球在 (2,1)”
3. **发给大模型**：大模型看完想：前面走不通，得先转向，于是回复 “右转”
4. **程序执行**：把 “右转” 换成数字 1，发给游戏
5. **游戏更新**：小人原地向右转，脸变成朝右，位置不变
6. 进入下一步，再来一遍

这就是你终端里，每一行输出背后发生的完整过程。

------

## 四、你之前遇到的两个问题，对应在哪

1. **一直原地不动**

   原因：大模型没意识到前面有障碍物，一直选 “前进”。游戏里前进被挡住了，小人位置不变，所以几十步都长一样。

   解决：在解说词里直接加上 “正前方有东西，不能前进”，大模型就知道要转向了。

2. **最后报错卡住**

   原因：调用大模型 API 时，网络连接失败 / SSL 握手失败，程序卡在那等回复。

   解决：加超时时间、换官方更稳定的 SDK，出错了也能继续跑，不卡死。